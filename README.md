# SafeShare

End-to-end encrypted file sharing between Android devices.

Upload a file from your phone, get a QR code (or 16-word BIP-39 phrase),
and anyone with the code can download and decrypt the file from any device.
The server stores only ciphertext and never sees the encryption key.
