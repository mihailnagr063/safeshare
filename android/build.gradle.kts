// Top-level build file. Plugin versions are declared here but the
// actual configuration lives in the :app module.

plugins {
    id("com.android.application") version "9.0.1" apply false
    id("com.android.library") version "9.0.1" apply false
}
